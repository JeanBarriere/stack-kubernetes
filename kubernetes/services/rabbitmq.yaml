apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: asyncy-platform
  labels:
    app: rabbitmq
spec:
  ports:
  - port: 5672
    protocol: TCP
    targetPort: ui
  selector:
    app: rabbitmq
